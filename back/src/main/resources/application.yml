spring:
  datasource:
    url: jdbc:mysql://localhost:3306/feelscore?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 416566066269-805128795551.apps.googleusercontent.com
            client-secret: GOCSPX-805128795551
            scope: [profile, email]
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Google
          naver:
            client-id: 805128795551
            client-secret: 805128795551
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: [name, email, profile_image]
            client-name: Naver
          kakao:
            client-id: 805128795551
            client-secret: 805128795551
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: [profile_nickname, account_email]
            client-name: Kakao
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  # AWS S3 설정 (local)
  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY_ID}
        secret-key: ${AWS_SECRET_ACCESS_KEY}
      region:
        static: ap-northeast-2
      s3:
        bucket: feelscore-s3

  # JWT Secret
  jwt:
    secret: ${JWT_SECRET}

logging:
  level:
    org.springframework.security: DEBUG
    com.my.jwt: DEBUG

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
# ===== prod =====
---
spring:
  config:
    activate:
      on-profile: prod
